public with sharing class MyCar {
    public void mycar(){
        string brand='Honda';
        string model='odyssey';
        string color='white';
        string properties=brand+','+model+','+color;
        system.debug(brand);
        system.debug(model);
        system.debug(color);
        system.debug(properties);
        
    }
}